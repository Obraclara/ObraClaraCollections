info:
  name: Create
  type: http
  seq: 3
  tags:
  - incidents
http:
  method: POST
  url: '{{baseUrl}}/api/v1/projects/:project_pk/incidents/'
  params:
  - name: project_pk
    value: ''
    type: path
  body:
    type: json
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
- name: 201 Response
  request:
    url: '{{baseUrl}}/api/v1/projects/:project_pk/incidents/'
    method: POST
    params:
    - name: project_pk
      value: ''
      type: path
    body:
      type: json
      data: |-
        {
          "id": "",
          "expected_created_by": "",
          "reported_date": "",
          "priority": "high",
          "incident_type": "materials",
          "impact_level": "minor",
          "description": "",
          "action_plan": "",
          "status": "pending",
          "responsible": "",
          "progress_entry": "",
          "edited_at": ""
        }
  response:
    status: 201
    statusText: Created
    headers:
    - name: Content-Type
      value: application/json
    body:
      type: json
      data: |-
        {
          "id": "",
          "reported_date": "",
          "priority": "high",
          "incident_type": "materials",
          "impact_level": "minor",
          "description": "",
          "action_plan": "",
          "status": "pending",
          "responsible": "",
          "progress_entry": "",
          "created_by": "",
          "created": "",
          "edited_at": "",
          "modified": "",
          "follow_ups_count": 0
        }
docs: |-
  CRUD for incidents scoped to a project.

  Idempotent create. The body may carry its own `id` (UUID v7). Without it,
  every request creates a new record. A retry with a stored `id` returns 200
  with the same payload. It skips validation and side effects.

  Optional `expected_created_by` must match the session user.

  Errors:
      - 400 bad_request: `id` is not a valid UUID v7
      - 403 owner_mismatch: `expected_created_by` differs from the session user
      - 409 id_conflict: the `id` belongs to another author or scope
      - 409 resource_deleted: the `id` is your own soft-deleted record (`details.deleted_at`)
script:
  post-response: |-
    const body = res.getBody();
    if (body.id) {
      bru.setEnvVar("incident_id", body.id);
    }
  tests: |-
    test("Status is 201", function() {
      expect(res.getStatus()).to.equal(201);
    });

    test("Response has id", function() {
      const body = res.getBody();
      expect(body.id).to.be.a("string");
    });
