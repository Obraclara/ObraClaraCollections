info:
  name: health
  type: folder
  seq: 12

request:
  auth: inherit

docs: |-
  Liveness probe for the container orchestrator. Served outside `/api/v1/`
  and it touches no database. Open to everyone, no role applies.
