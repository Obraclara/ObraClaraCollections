info:
  name: Create
  type: http
  seq: 4
  tags:
  - expenses
http:
  method: POST
  url: '{{baseUrl}}/api/v1/projects/:project_pk/expenses/'
  params:
  - name: project_pk
    value: ''
    type: path
  body:
    type: json
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
examples:
- name: 201 Response
  request:
    url: '{{baseUrl}}/api/v1/projects/:project_pk/expenses/'
    method: POST
    params:
    - name: project_pk
      value: ''
      type: path
    body:
      type: json
      data: |-
        {
          "progress_entry": "",
          "supplier": "",
          "base_category": null,
          "custom_category": null,
          "amount": "",
          "concept": "",
          "date": "",
          "visible_to_client": false
        }
  response:
    status: 201
    statusText: Created
    headers:
    - name: Content-Type
      value: application/json
    body:
      type: json
      data: |-
        {
          "id": "",
          "progress_entry": "",
          "supplier": "",
          "category": {
            "source": "",
            "code": null,
            "id": null,
            "name": "",
            "image": null
          },
          "amount": "",
          "concept": "",
          "date": "",
          "visible_to_client": false
        }
docs: |-
  CRUD for expenses scoped to a project.
  Set either `base_category` (bundled enum, 1-7) or `custom_category`
  (project category UUID) — they are mutually exclusive; setting one clears
  the other. The response `category` is a normalized object
  `{source, code, id, name, image}` (null when uncategorized).
script:
  post-response: |-
    const body = res.getBody();
    if (body.id) {
      bru.setEnvVar("expense_id", body.id);
    }
  tests: |-
    test("Status is 201", function() {
      expect(res.getStatus()).to.equal(201);
    });

    test("Response has id", function() {
      const body = res.getBody();
      expect(body.id).to.be.a("string");
    });
